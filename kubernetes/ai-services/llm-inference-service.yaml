apiVersion: v1
kind: Service
metadata:
  name: llm-inference
  namespace: ai-services
spec:
  type: ClusterIP
  selector:
    app: llm-inference
  ports:
    - name: http
      port: 80
      targetPort: 8001
